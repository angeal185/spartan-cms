{% import "macros.njk" as macro %}
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta content="width=device-width,initial-scale=1" name=viewport>
    <link rel="shortcut icon" href="/images/lg_50.png">
    <title>login</title>
    {% for i in['admin', 'fa','login'] %}
      {{macro.link(i)}}
    {% endfor %}
    <script>
      const appconf = {{data|dump|safe}}
    </script>
  </head>
  <body id="page-top">
      {{macro.script(title)}}
  </body>
</html>
